#include "header_general.h"

int main() {
    setbuf(stdout, 0);
    char* jeu_donnees = "01";
    DATAS datas = SCANDATA(jeu_donnees);
    for(int i = 0; i < datas.EXCLUSION_TOT; i++){

    }
    return 0;
}
